/**
 * Pricing / service catalog.
 * Admin edits prices and services here; nothing else in the codebase
 * hard-codes a fee.
 *
 * Satisfies: "pricing should NOT be hard-coded" (spec §21) and
 * "admin can add new services without rebuilding the app" (spec §28).
 */
package com.taxedge.catalog;
